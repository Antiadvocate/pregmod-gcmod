App.Patch.register({
	releaseID: 1291,
	descriptionOfChanges: "Remove ProstheticsAdjustCompleted variable.",
	// does nothing, it'll get autoshredded (but breaks compatibility, so we have to notify, so we have to patch...)
});
